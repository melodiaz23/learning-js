# TS

- A superset to JS.
- Add static types
- Adds some features
- Compiles next-gen JS to 'old JS'

To compile:
  tsc app.ts
  tsc --init
  tsc // compile all the files

**Resources**
https://www.typescriptlang.org/